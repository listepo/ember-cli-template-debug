import { debugConsole } from '../../../helpers/debug-console';
import { module, test } from 'qunit';

module('Unit | Helper | debug console');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = debugConsole(42);
  assert.ok(result);
});
